import React from "react";
import SearchBar from "../components/SearchBar"; // Adjust the path if necessary

const VoterIDSearchPage = () => {
    return (
        <div>
           
            <SearchBar />
        </div>
    );
};

export default VoterIDSearchPage;
